На основе предоставленных материалов, ваша задача заключается в программной реализации синтеза логической схемы конечного автомата (КА).Ниже приведен структурированный текст задачи, который послужит техническим заданием (ТЗ) для написания кода.Техническое задание: Реализация генератора таблиц конечного автомата1. Цель работыРазработать программу для автоматического формирования сводной таблицы истинности (таблицы переходов и выходов) для конечного автомата Мили на основе заданного графа.2. Входные данныеПрограмма должна принимать или позволять пользователю задать:Множество состояний ($S$): Минимум 5 состояний (например, $S_0, S_1, S_2, S_3, S_4$).Входные сигналы ($X$): Минимум 3 различных сигнала ($X_0, X_1, X_2...$).Выходные сигналы ($Y$): Минимум 6 различных реакций ($Y_0, Y_1, Y_2...$).Граф переходов: Описание того, из какого состояния $S_i$ при каком входе $X_j$ автомат переходит в состояние $S_k$ и какой выход $Y_m$ при этом генерируется.3. Алгоритм работы программыДля реализации кода вам необходимо последовательно выполнить следующие шаги:Кодирование элементов (перевод в двоичную систему):Присвоить каждому состоянию $S$ уникальный двоичный код $Q$ (например, $S_0 = 000, S_1 = 001$ и т.д.).Закодировать входные сигналы $X$ двоичными значениями.Закодировать выходные сигналы $Y$ двоичными значениями.Построение таблицы переходов-реакций:Создать матрицу, где строки — это входные сигналы ($X$), а столбцы — текущие состояния ($S$).В ячейках указать пару: Следующее состояние / Выходной сигнал.Формирование сводной таблицы (Truth Table):Разложить закодированные значения на отдельные биты:Входы: текущее состояние ($Q_1, Q_2, Q_3$) и входной сигнал ($x_1, x_2$).Выходы: следующее состояние ($q_1, q_2, q_3$) и функции выходов ($y_1, y_2, y_3$).Заполнить таблицу для всех возможных комбинаций входных переменных. Если переход не задан, использовать символ неопределенности («*»).4. Ожидаемый результат (Output)Программа должна выводить в консоль или файл итоговую таблицу следующего вида:X (вход)Q (тек. сост.)q (след. сост.)Y (выход)биты $x_i$биты $Q_i$биты $q_i$биты $y_i$(Пример заполнения: если в $S_0(000)$ пришел $X_1(01)$, и переход ведет в $S_1(001)$ с выходом $Y_1(001)$, строка таблицы будет содержать эти биты).Подсказка для реализации кода:Вам лучше всего использовать структуру данных «словарь» (dictionary) или «класс», где ключом будет пара (текущее_состояние, входной_сигнал), а значением — объект, содержащий (новое_состояние, выход). Это позволит легко итерироваться по всем комбинациям для создания финальной таблицы.

реализуй алоритм на python, чтобы я мог на случайном графе со стрелочками добавлять им иксы и игрити, а затем программа выводила все таблицы, вкдючая итоговую
